name: Glossary Checksum

on:
  push:
    paths:
      - 'glossary.md'
      - 'glossary.json'
  pull_request:
    paths:
      - 'glossary.md'
      - 'glossary.json'

permissions:
  contents: read

jobs:
  checksum:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Verify glossary checksum
        run: |
          echo "Verifying glossary file integrity..."
          sha256sum glossary.md
          sha256sum glossary.json
