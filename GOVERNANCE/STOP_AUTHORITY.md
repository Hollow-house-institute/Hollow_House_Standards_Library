# Stop Authority

Stop immediately if:
- `glossary.json` is missing
- `CHECKSUMS.sha256` is missing
- `AUTHORITY.md` is missing
- `CODEOWNERS` is missing
- uncontrolled duplicate governance paths exist
