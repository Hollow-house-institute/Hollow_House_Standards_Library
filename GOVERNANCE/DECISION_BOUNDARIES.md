# Decision Boundaries

- If `glossary.json` changes, `CHECKSUMS.sha256` must also change.
- If ownership is missing, changes are invalid.
- If required authority files are missing, stop changes.
- This repository defines terminology and authority integrity only.
- Runtime enforcement belongs downstream, not in this repository.
